---
description: SN Cleaning admin UI shell design principles — layout, surfaces, primitives
globs: src/pages/**/*.tsx,src/layouts/shell/**,src/components/**/list/**
alwaysApply: false
---

# UI design principles

Full reference: `.cursor/design-principles.md`

## Rules

1. **One surface** — main content card in `AppShell` is the only page card. No nested `Card` wrappers for page sections.
2. **Flat sections** — `ShellSectionHeader` + hairline dividers between major blocks.
3. **Shell primitives** — `ShellPage`, `ShellSectionHeader`, `ShellStatGrid`, `ShellSegment`, list/notification components.
4. **Exemplar pages** — `Dashboard.tsx` (layout); `Users.tsx` + `users/list/*` (Tailwind chrome).
5. **Tables/lists** — do not restyle table rows when doing page chrome work; table unification is separate.
6. **No legacy chrome** — use `ShellSegment`, not shadcn `Tabs`. No heavy `rounded-3xl` shadow stacks.

## Styling (single source of truth)

- **Use Tailwind** on components for all styles.
- **Tokens** in `tailwind.config.ts` (`shell.*`) + `index.css` (`--shell-*`, shadcn vars).
- Shell CSS files are deleted; collapsed-sidebar tooltips live in `index.css` `@layer components` only.
